首先，有一个配置文档，里面有调用指南和密钥。但是你要做的不是针对这一家平台进行配置，而是能自由切换厂商，配置api以及密钥。具体可以参考cherry studio实现方式。

但测试阶段，你可以只使用该模型进行测试，跑通后再去扩展。

这里面的大模型主要用来做生成文本，或者总结文本的作用。

目前该项目的业务主要是github的每日提交。

这个文件夹的业务可能会扩展，但是目前业务是gitHub的每日签到（每天会运行该项目，该项目会利用提示词，引导AI生成一篇代码（或者是算法解析，或者是一门新的技术，或者是一篇专业介绍），或者干脆使用提示词引导AI让它生成一个主题），然后将之上传到github。

所以初始的时候，应该会要求，github的登录凭证，github仓库地址等一切需要绑定的东西。

目前只需要cli。